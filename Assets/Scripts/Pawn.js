// #pragma strict

// function Start () {

// 	object[] obj = GameObject.FindObjectsOfType(typeof (GameObject));
//   	foreach (object o in obj)
//   {
//        GameObject g = (GameObject) o;
//        Debug.Log(g.name);
//   }

// }

// function Update () {
		
	// if(Input.GetMouseButton(0))
 //   //move upon click
	// {

	// 	// Pawn can only move in the Z direction either one block or two blocks.

	// 	//var currentPos = transform.position.z;
	// 	//currentPos = currentPos + 0.1
		

// 		//transform.position = Vector3(transform.position.x,transform.position.y,Input.mousePosition.y-50.6);
// 		//Debug.Log(transform.position);
// 		//transform.position.z = Input.mousePosition.z;
// 	}

// }
